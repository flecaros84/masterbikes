package masterbikes.venta_service.dto;

import lombok.Data;

@Data
public class InventarioDTO {
    private Long id;
    // puedes añadir otros campos si los necesitas, pero para buscar sólo
    // con el id basta así
}